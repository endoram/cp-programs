#The point of setup.sh is to install the nessisary packages needed to run scoregen.py and scorecard.py correctly
#They might have what they already need to run it but this checks and does it anyway

#This installs python3
sudo apt-get install python3

#This is installs tkinter
sudo apt-get install python-tk

#This installs mpg123
sudo apt-get install mpg123
